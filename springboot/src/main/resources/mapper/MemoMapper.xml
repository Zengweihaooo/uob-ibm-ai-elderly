<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.MemoRepository">

    <!-- Result mapping -->
    <resultMap id="MemoResultMap" type="com.example.demo.pojo.Memo">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="is_important" property="isImportant" jdbcType="BOOLEAN"/>
        <result column="pin_code" property="pinCode" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="is_deleted" property="isDeleted" jdbcType="BOOLEAN"/>
    </resultMap>

    <!-- Insert memo -->
    <insert id="insert" parameterType="com.example.demo.pojo.Memo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO memos (
            user_id, title, content, type, is_important, pin_code, 
            create_time, update_time, is_deleted
        ) VALUES (
            #{userId}, #{title}, #{content}, #{type}, #{isImportant}, #{pinCode},
            #{createTime}, #{updateTime}, #{isDeleted}
        )
    </insert>

    <!-- Save memo (save method) -->
    <insert id="save" parameterType="com.example.demo.pojo.Memo" useGeneratedKeys="true" keyProperty="id">
        INSERT OR REPLACE INTO memos (
            id, user_id, title, content, type, is_important, pin_code, 
            create_time, update_time, is_deleted
        ) VALUES (
            #{id}, #{userId}, #{title}, #{content}, #{type}, #{isImportant}, #{pinCode},
            #{createTime}, #{updateTime}, #{isDeleted}
        )
    </insert>

    <!-- Update memo -->
    <update id="update" parameterType="com.example.demo.pojo.Memo">
        UPDATE memos 
        SET title = #{title},
            content = #{content},
            type = #{type},
            is_important = #{isImportant},
            pin_code = #{pinCode},
            update_time = #{updateTime}
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </update>

    <!-- Find memo by ID -->
    <select id="findById" parameterType="long" resultMap="MemoResultMap">
        SELECT * FROM memos WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- Find undeleted memo by user ID and memo ID -->
    <select id="findByUserIdAndId" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND id = #{memoId} AND is_deleted = 0
    </select>

    <!-- Find all undeleted memos by user ID -->
    <select id="findByUserId" parameterType="long" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY update_time DESC
    </select>

    <!-- Find undeleted memos by user ID and type -->
    <select id="findByUserIdAndType" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND type = #{type} AND is_deleted = 0
        ORDER BY update_time DESC
    </select>

    <!-- Find important memos by user ID -->
    <select id="findImportantByUserId" parameterType="long" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND is_important = 1 AND is_deleted = 0
        ORDER BY update_time DESC
    </select>

    <!-- Find memos by user ID and PIN code -->
    <select id="findByUserIdAndPinCode" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND pin_code = #{pinCode} AND is_deleted = 0
        ORDER BY update_time DESC
    </select>

    <!-- Search memos (title or content contains keyword) -->
    <select id="searchByKeyword" resultMap="MemoResultMap">
        SELECT * FROM memos 
        WHERE user_id = #{userId} AND is_deleted = 0
        AND (LOWER(title) LIKE '%' || LOWER(#{keyword}) || '%' 
             OR LOWER(content) LIKE '%' || LOWER(#{keyword}) || '%')
        ORDER BY update_time DESC
    </select>

    <!-- Soft delete memo -->
    <update id="softDelete">
        UPDATE memos 
        SET is_deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId} AND id = #{memoId} AND is_deleted = 0
    </update>

    <!-- Count memos by user ID -->
    <select id="countByUserId" parameterType="long" resultType="long">
        SELECT COUNT(*) FROM memos 
        WHERE user_id = #{userId} AND is_deleted = 0
    </select>

    <!-- Count important memos by user ID -->
    <select id="countImportantByUserId" parameterType="long" resultType="long">
        SELECT COUNT(*) FROM memos 
        WHERE user_id = #{userId} AND is_important = 1 AND is_deleted = 0
    </select>

    <!-- Count memos by user ID and type -->
    <select id="countByUserIdAndType" resultType="long">
        SELECT COUNT(*) FROM memos 
        WHERE user_id = #{userId} AND type = #{type} AND is_deleted = 0
    </select>

    <!-- Check if user has memos with the specified PIN code -->
    <select id="existsByUserIdAndPinCode" resultType="boolean">
        SELECT COUNT(*) > 0 FROM memos 
        WHERE user_id = #{userId} AND pin_code = #{pinCode} AND is_deleted = 0
    </select>
    
    <!-- Get all memos (for data migration) -->
    <select id="findAll" resultMap="MemoResultMap">
        SELECT * FROM memos ORDER BY update_time DESC
    </select>
    
    <!-- Count total memos (for data migration) -->
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM memos
    </select>

</mapper> 