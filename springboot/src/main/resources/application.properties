# Spring Boot application configuration for IBM AI Elderly Project
spring.application.name=demo

# ==================== SQLite Database Configuration ====================
# SQLite数据库配置 - 本地文件存储，适合老年人设备部署

# Database Connection / 数据库连接
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.url=jdbc:sqlite:data/elderly_companion.db
spring.datasource.username=
spring.datasource.password=

# HikariCP Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=1

# JPA Configuration / JPA配置
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.community.dialect.SQLiteDialect

# Database File Management / 数据库文件管理
app.database.path=data/elderly_companion.db
app.database.backup.path=data/backups/
app.database.init-schema=true

# MyBatis Configuration
mybatis.mapper-locations=classpath*:mapper/*.xml
mybatis.type-aliases-package=com.example.demo.pojo

# ==================== Email Configuration ====================
# SMTP Email Configuration for 163.com
spring.mail.host=smtp.163.com
spring.mail.port=465
spring.mail.username=elderapp2025@163.com
spring.mail.password=RAAvi37nyRv2dhS2
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.default-encoding=UTF-8

# Thymeleaf template configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# SMS Configuration for IBM AI Elderly Project
# SMS service configuration - supports both mock and real SMS sending

# Mock mode setting (true=mock sending, false=real sending)
app.sms.mock=true

# SMS service provider (mock/twilio)
app.sms.provider=mock

# Twilio configuration (used when provider=twilio)
# Note: Replace the following values with your real Twilio credentials
# How to get: Login to https://console.twilio.com/ and check Dashboard
app.sms.twilio.account-sid=your_twilio_account_sid_here
app.sms.twilio.auth-token=your_twilio_auth_token_here

# Twilio sender phone number (needs to be verified or purchased in Twilio console)
# Trial accounts can use Twilio provided test numbers
app.sms.twilio.from-number=+15551234567


# === Social Pet MVP ===
pet.wakeupHour=8                 # Hour for morning greetings
pet.defaultPodcastTime=15:00     # Default podcast time
pet.quietHours.start=21:00       # Do-not-disturb start time
pet.quietHours.end=07:00         # Do-not-disturb end time

# Optional: Use cron for precise task scheduling
pet.morning.cron=*/10 * * * * *  # Execute every 10 seconds (for testing)
pet.weeklyReport.cron=0 0 9 * * MON # Execute every Monday at 9:00 AM

